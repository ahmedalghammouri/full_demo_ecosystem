shamefully-hoist=true
node-linker=hoisted

# Network resilience for Docker builds — registry.npmjs.org can be flaky from the
# VPS, and a single metadata/tarball timeout otherwise fails the whole build.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
